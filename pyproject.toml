[project]
name = "inlier-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pooch>=1.6.0",
]

[tool.setuptools]
py-modules = ['inlier_data']

